{
  "name": "3DFlus-framework-benchmark",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "gh-pages": "^3.2.3"
  },
  "scripts": {
    "build": "(cd src/deckgl/1000points && npm run build); (cd src/deckgl/XXXXpoints && npm run build);",
    "deploy": "gh-pages -b gh-pages -d dist"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
